<local:LabPanelBase x:Class="MvvmLabApp.CounterPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MvvmLabApp"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Height="200" Width="300">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Counter Panel" FontSize="16" FontWeight="Bold" Grid.ColumnSpan="2" />
        <TextBlock Text="Current Value:" Grid.Row="1" />
    <TextBlock Text="{Binding VM.Value}" Grid.Row="1" Grid.Column="1" />

        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,5,0,0">
            <Button Content="Increment" Width="90" Click="Increment_Click" Margin="0,0,6,0" />
            <Button Content="Save" Width="60" Click="Save_Click" Margin="0,0,6,0" />
            <Button Content="Close" Width="60" Click="Close_Click" />
        </StackPanel>

        <Border Grid.Row="3" Grid.ColumnSpan="2" Margin="0,8,0,0" BorderBrush="LightGray" BorderThickness="1" Padding="4">
            <StackPanel>
                <TextBlock Text="Settings (bound two-way):" FontWeight="Bold" />
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="Increment Amount:" VerticalAlignment="Center" Margin="0,0,4,0" />
                    <TextBox Width="40" Text="{Binding Settings.IncrementAmount, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</local:LabPanelBase>