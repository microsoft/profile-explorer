<local:ToolPanelControl x:Class="IRExplorerUI.Profile.ProfileReportPanel"
                        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                        xmlns:local="clr-namespace:IRExplorerUI"
                        mc:Ignorable="d" 
                        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Grid>
                <TextBlock Text="Process"/>
                <TextBox Margin="100,0,0,0"  Text="{Binding Process.Name}" IsReadOnly="True"/>
            </Grid>
            <Grid>
                <TextBlock Text="Image file"/>
                <TextBox Margin="100,0,0,0"  Text="{Binding Process.ImageFileName}" IsReadOnly="True"/>
            </Grid>
            <Grid>
                <TextBlock Text="Cmd line"/>
                <TextBox Margin="100,0,0,0"  Text="{Binding Process.CommandLine}" IsReadOnly="True"/>
            </Grid>
        </StackPanel>
    </Grid>
</local:ToolPanelControl>
